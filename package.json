{
  "name": "docker-api-list-containers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker build -t dlsc .",
    "start": "docker run -v /var/run/docker.sock:/var/run/docker.sock -it dlsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dockerode": "^3.3.4"
  }
}
